<%- include('../index.ejs')%>

<header>
    <% if(user) { %>
    <div>
        <a class="home" href="/">Home</a>

    </div>

        <h3><%= pageTitle %></h3>

        <span>
        <a href="user/logout" class="logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>

        </span>

        <input type="hidden" value="<%= user.userName %>" name="loggedUser"/>
        <% } %>
</header>

<div class="whole-container">


